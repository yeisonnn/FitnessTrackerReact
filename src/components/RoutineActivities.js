import React from 'react';

const RoutineActivities = (props) => {
 const {privateRoutines} = props

    return(
        <div>
            <form>

            </form>
        </div>

    )
}

export default RoutineActivities